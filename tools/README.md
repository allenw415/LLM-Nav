# Tools

Non-runtime tooling used around the navigation system.

- `evaluation/`: benchmark and evaluation entrypoints.
- `pano_viewer/`: panorama graph viewer source and exporter.
- `data/`: dataset preparation and grounding tools.

Runtime navigation code should stay in `st_nav/`; examples should stay in
`examples/`.
